# v0.2.5

MCP documentation and smoke verification for AI-assisted modernization workflows.

## MCP docs and smoke verification

Document and verify the full MCP tool surface.

Audience: AI client integrators and consultants validating EMP MCP workflows

- Adds `docs/mcp.html` with the recommended MCP workflow, tool inputs, JSON-RPC examples, and local verification command.
- Adds `npm run mcp:verify` to smoke-test `initialize`, `tools/list`, `emp.packs`, `emp.benchmarks`, `emp.analyze`, and `emp.transformPlan`.
- Wires MCP verification into `npm run check`.
- Links the MCP guide from the generated documentation home page and README.

Links:
- [MCP guide](docs/mcp.html)
- [MCP usage](README.md#cli)
